#include "../include/Harris.h"

#include <cuda.h>

void harris::getHarrisValues(const cv::Mat& in,
                             const size_t windowSize,
                             const double gaussianSigma,
                             cv::Mat& harrisVals) {
    ;
}